import { BaseRedirectParams, LoginParams, OpenLoginOptions, OpenloginSessionData, OpenloginUserInfo, SocialMfaModParams } from "@toruslabs/openlogin-utils";
declare class OpenLogin {
    state: OpenloginSessionData;
    options: OpenLoginOptions;
    private sessionManager;
    private currentStorage;
    private _storageBaseKey;
    private dappState;
    private addVersionInUrls;
    constructor(options: OpenLoginOptions);
    get privKey(): string;
    get coreKitKey(): string;
    get ed25519PrivKey(): string;
    get coreKitEd25519Key(): string;
    get sessionId(): string;
    get sessionNamespace(): string;
    get appState(): string;
    private get baseUrl();
    init(): Promise<void>;
    login(params: LoginParams & Partial<BaseRedirectParams>): Promise<{
        privKey: string;
    } | null>;
    logout(): Promise<void>;
    enableMFA(params: Partial<LoginParams>): Promise<boolean>;
    manageMFA(params: Partial<LoginParams>): Promise<void>;
    changeSocialFactor(params: SocialMfaModParams & Partial<BaseRedirectParams>): Promise<boolean>;
    getUserInfo(): OpenloginUserInfo;
    private createLoginSession;
    private _authorizeSession;
    private updateState;
    private rehydrateSession;
    private openloginHandler;
}
export default OpenLogin;
