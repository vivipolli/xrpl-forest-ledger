import { BaseSessionManager } from "@toruslabs/base-session-manager";
import { OpenloginSessionManagerOptions } from "./interfaces";
export declare class OpenloginSessionManager<T> extends BaseSessionManager<T> {
    sessionServerBaseUrl: string;
    sessionNamespace: string;
    sessionTime: number;
    sessionId: string;
    constructor({ sessionServerBaseUrl, sessionNamespace, sessionTime, sessionId }?: OpenloginSessionManagerOptions);
    static generateRandomSessionKey(): string;
    createSession(data: T): Promise<string>;
    authorizeSession(): Promise<T>;
    updateSession(data: Partial<T>): Promise<void>;
    invalidateSession(): Promise<boolean>;
}
