import type { IPhantomWalletProvider } from "@web3auth/solana-provider";
declare global {
    interface Window {
        solana: IPhantomWalletProvider & {
            isPhantom: boolean;
        };
    }
}
export declare function poll(callback: () => boolean | Promise<boolean>, interval: number, count: number): Promise<boolean>;
export declare const detectProvider: (options?: {
    interval: number;
    count: number;
}) => Promise<IPhantomWalletProvider | null>;
